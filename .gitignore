# OS / editor
.DS_Store
Thumbs.db
.thumbnail
*.swp
.vscode/
.idea/

# Node (in case anyone adds tooling later)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build output (none right now — kept for future)
dist/
build/

# Local-only: Claude Designs source, kept for reference — not deployed
_legacy/

# Local-only: snapshot of the old React `main` branch for SEO reference
_main_snapshot/
